[pytest]
testpaths = tests
pythonpath = .
addopts = -q
markers =
    integration: talks to a live Ollama server; skipped automatically when it is not running
filterwarnings =
    ignore::DeprecationWarning
